export default function DragDropQuestion({ draggableItems, dropZones, questionNumber, questionTitle }) {
  // منطق السحب والإفلات
  return (
    <div className="drag-drop-container">
      {/* تصميم السحب والإفلات */}
    </div>
  );
}
